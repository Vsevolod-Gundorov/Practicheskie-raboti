package com.company;

public class createComplex implements Complex
{
    public createComplex(int image, int real)
    {
        int a = image;
        int b = real;
        System.out.println(a + " + " + b + "i");
    }
}
